import { Vector3 } from "./Vector3";

/**
 * Returns a Vector3 representing the world space direction in which the Camera is looking.
 * @param target - The result will be copied into this Vector3.
 * @returns The target vector with the world direction of the camera.
 */
export function getWorldDirection(target: Vector3): Vector3 {
  // Implementation would go here
  return target;
}
